compute_unit: "A compute unit (CU) performs a specific computation. It is realized as a function parameter structure (FPS) connected to a configuration and a run process. The two processes run inside a tmux session."
